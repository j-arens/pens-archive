document.querySelector('button').addEventListener('click', () => {
  // const hamburger = document.querySelector('.hamburger');
  document.body.classList.toggle('menu-active');
  
  // hamburger.querySelectorAll('path').forEach(path => {
  //   path.getAttribute('d');
  // });
});
